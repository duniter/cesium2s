fragment LightIdentity on Identity {
  id
  name
  isMember
  accountId
  membershipHistory {
    __typename
    id
  }
}

fragment LightAccount on Account {
  id
  identity {
    ...LightIdentity
  }
}
