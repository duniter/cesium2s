
fragment LightIdentity on Identity {
  id
  name
  isMember
  account {
    id
  }
  membershipHistory {
    __typename
    id
  }
}

fragment LightAccount on Account {
  id
  identity {
    id
    name
    isMember
    membershipHistory {
      id
    }
  }
}
